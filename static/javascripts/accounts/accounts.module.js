(function () {
  'use strict';

  angular
    .module('wingrade.accounts', [
      'wingrade.accounts.controllers',
      'wingrade.accounts.services'
    ]);

  angular
    .module('wingrade.accounts.controllers', []);

  angular
    .module('wingrade.accounts.services', []);
})();
